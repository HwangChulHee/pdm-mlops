# k8s/prometheus-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data
  namespace: pdm
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi